# ShieldSync Enterprise - Continue Here (2026-07-10)

Handoff after: visual/UX/a11y audit sweep + verdict-first demo-report redesign +
"Main site" link fix + marketing free-labs regrid.

## TL;DR
- Visual sweep + verdict-first demo report: **COMMITTED** (`6e4b9b8`), enterprise
  prod deploy **HELD** (see below).
- Free-labs 2-up regrid: **LIVE** on shieldsyncsecurity.com/free-labs (`e99504e`, Amplify).
- Positioning locked: B2B hiring labs are a separate, **level-graded** track
  (Cloud Security Analyst -> Engineer -> Sr Engineer -> Architect -> Manager),
  never reusing B2C/learner content.

## #1 thing to decide: the enterprise deploy
`6e4b9b8` (this session's visual work) is committed but the enterprise app is
NOT deployed, because the working tree also holds a large UNCOMMITTED
parallel-build batch that a `cf:deploy` would ship half-finished:

- **engine:** `engine/ent-handler.mjs`, `engine/entinfra.mjs`,
  `engine/deploy/policy-ent.json`, `engine/create-ent-audit-table.mjs` (untracked)
- **app (modified):** `app/portal/assessments/[id]/{invites-table,page}.tsx`,
  `app/portal/assessments/new/new-assessment-form.tsx`,
  `app/admin/orgs/[orgId]/page.tsx`, `app/api/portal/assessments/route.ts`
- **app (untracked):** `app/portal/assessments/[id]/assessment-settings.tsx`,
  `app/api/portal/{assessments/update,invites/resend}/`,
  `app/api/admin/{audit,invoice}/`,
  `app/admin/orgs/[orgId]/{_components,orders}/`, `lib/labs.ts`

`npx tsc --noEmit` across the whole tree is currently **exit 0** (mine + theirs compile).

**To ship the enterprise app once that wave is ready:**
1. Review + commit the parallel wave (engine .mjs + portal/admin files above).
2. Engine (only if its .mjs changed and are ready; approval-gated): build the
   5-mjs + labs/ zip, then
   `aws --profile ent750 lambda update-function-code --function-name ShieldSyncEnterpriseEngine --zip-file fileb://<zip>`
   (preserves env + secret; do NOT re-run deploy-ent.ps1 without the current secret).
3. App: `cd enterprise && npm run cf:deploy` (or push to master -> CI).
4. Verify `/demo/report` + `/a/[token]` on prod.

To ship my visual work *now, as-is* (accepting the parallel wave goes live too):
just run step 3.

## What changed this session

### Committed `6e4b9b8` (enterprise, deploy-held)
- **Demo report `/demo/report` - verdict-first flow.** New "At a glance" card
  up top: leader + field summary ("Priya S. leads with 6 of 6 objectives ...;
  4 of 6 completed; scores 33%-100%, avg 71%; 1 passed every objective") -
  **verified facts only, no hire/no-hire call** (honest-MVP intact). Dropped the
  DUPLICATE "How this is scored" banner (was rendering twice); it now shows once,
  right before the ranked table. Order: header -> params -> verdict -> stats ->
  how-scored -> ranked table -> candidate drill-down -> CTA.
- **"Main site" link.** Report chrome (`app/r/_components/report-bits.tsx`
  `ReportShell`) -> **"Enterprise home" (`/`)** so a standalone report has an
  obvious path back into the product. Homepage header
  (`components/marketing/site-header.tsx`) cross-link relabeled to
  **"shieldsyncsecurity.com"** (disambiguates from enterprise home). Logo still -> `/`.
- **Visual/a11y sweep (4 parallel opus agents, all tsc-clean):**
  - **Candidate room** (`app/a/[token]/candidate-flow.tsx`): brand mark, OTP +
    reflection aria-labels + `autocomplete="one-time-code"`, focus rings,
    early-submit "Back to the task" safety button, resend-code feedback, rose
    error color, `rounded-lg` buttons, consent sub-processors "AWS, Cloudflare,
    and Google (Gemini)", slot picker radiogroup.
  - **Marketing home** (`app/page.tsx`, `app/layout.tsx`, `site-header`,
    `site-footer`, `product-tour`): hero H1 cap, comparison "No" mark contrast,
    table `scope`, skip-to-content link, header/footer focus rings, product-tour
    mobile tab labels + proper tab ARIA (removed the aria-live auto-advance spam).
  - **Admin** (`app/admin/_components/admin-nav.tsx`, `app/admin/page.tsx`): nav
    wrap + real Logo + focus rings, orgs table horizontal scroll + `scope`.
  - **Report/demo** (`report-bits.tsx`, `app/r/c/[token]/page.tsx`, demo): dropped
    the permanent "Finalizing" placeholder cards (correctness-only MVP), stat-size
    caps, heading order, RankBadge sr-only "Rank N".

### Live `e99504e` (marketing repo shieldsync-website/, Amplify)
- `/free-labs`: full-width stacked rows -> **2-up card grid** (1-up mobile, 2-up
  tablet+, equal-height, CTAs bottom-aligned). Capped at 2 cols while there are 2
  live labs (no lonely empty slot); switch to `lg:grid-cols-3` + `max-w-5xl` when
  a 3rd free lab ships (commented in `app/free-labs/page.tsx`). Verified live in Chrome.

## Positioning (saved to memory: project-shieldsync-enterprise-labs)
B2B hiring assessment uses its OWN comprehensive, **level-graded** labs - a
distinct scenario per level, matched to the seniority being hired for:
1. Cloud Security Analyst
2. Cloud Security Engineer
3. Sr Cloud Security Engineer
4. Cloud Security Architect / Sr Cloud Security Architect
5. Cloud Security Manager

Never reuse a B2C/learner lab (would under- or over-test). Anchor the demo report
and product copy to a specific level.

## Remaining work
- **Portal Batch 4 (visual audit) - NOT done here** (the parallel session owns
  portal files). Flagged items: revoke inline 2-step confirm, status-badge
  contrast, "Completed" -> "Submitted" relabel, dashboard/billing table
  `overflow-x-auto` wrappers, "Saved" clears via setTimeout, CopyButton aria-live,
  email consistency (hello@ -> info@), th `scope`, sign-out pending state.
- **Task #4 - Lead capture / Book a demo:** replace the mailto CTAs with a real
  form + engine route/table. Top-of-funnel priority.
- **Task #7 - Azure GUI assessment:** blocked on owner creating the labs Entra
  tenant + secrets; engine `mintAccess` is already Portal-ready.
- **Owner-gated (unchanged):** SES production access (case 178347826400236), MFA
  enforce on the enterprise pool, agreement legal-text review,
  `SHIELDSYNC_GSTIN`/`SHIELDSYNC_ADDRESS` env before real invoicing.

## Key facts / commands
- **Enterprise app:** `enterprise/` - Next 16, CF Workers/OpenNext. Prod build MUST
  be `next build --webpack` (Turbopack breaks OpenNext). Dev port 3002. Deploy:
  `npm run cf:deploy` or push master.
- **Enterprise engine:** `ShieldSyncEnterpriseEngine` Lambda (acct 750), API GW
  `https://bdkdcbhzme.execute-api.us-east-1.amazonaws.com`. Deploy via
  `aws --profile ent750 lambda update-function-code` (approval-gated;
  `--profile ent750` assumes OrganizationAccountAccessRole into 750294427884).
- **Marketing site:** `shieldsync-website/` - Next static export -> AWS Amplify.
  Deploy = commit + push `main`; verify first with `STATIC_EXPORT=true npm run build`.
