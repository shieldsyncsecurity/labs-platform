// TODO: build the real sample-report view (read-only walkthrough of a graded
// assessment: findings, scoring rubric, timeline). Deferred — this is a
// placeholder so the landing page's "View a sample report" link resolves.
export default function DemoReportPage() {
  return (
    <div className="mx-auto max-w-2xl px-6 py-24 text-center">
      <h1 className="text-2xl font-bold text-ink">Sample report — coming soon</h1>
      <p className="mt-3 text-ink-soft">
        This page will show a real, anonymized assessment report. For now, book a walkthrough and
        we will walk you through one live.
      </p>
    </div>
  );
}
