{
  "name": "shieldsync-labs-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "ShieldSync Labs Session Engine (LabInfra + provisioning). Runs locally now (chaining into the platform account); becomes a Lambda in the platform account later.",
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-budgets": "^3.1063.0",
    "@aws-sdk/client-cloudformation": "^3.700.0",
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/client-iam": "^3.700.0",
    "@aws-sdk/client-organizations": "^3.700.0",
    "@aws-sdk/client-s3-control": "^3.700.0",
    "@aws-sdk/client-sts": "^3.700.0"
  }
}
